#include<stdio.h>
#include<string.h>
int main()
{
	int n;
	scanf("%d", &n);
	for(; n > 0; n--)
	{
		char a[100];
		scanf("%s", a);
		int length = strlen(a) -2;
		if(length > 8)
		{
		printf("%s%d%s", a[0], length, a[strlen(a)-1]);
		}
		else
		{
		printf("%s", a);
		}
	}
	
	return 0;
 } 
